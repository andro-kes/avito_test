# ✅ CHECKLIST — Backend Intern Test Task

Перед сдачей убедись, что всё из списка ниже выполнено.  
Если пункт не применим — оставь комментарий в PR, почему.

---

## ⚙️ Общая структура проекта

- [ ] Код запускается командой **`docker compose up`** без ошибок  
- [ ] Сервис доступен на **`localhost:8080`**  
- [ ] Есть **`README.md`** с инструкцией по запуску  
- [ ] Есть **`Makefile`** или чётко описанные команды сборки  
- [ ] Используется структура `/cmd`, `/internal`, `/pkg`  
- [ ] Есть миграции в `/migrations` и они применяются при старте  
- [ ] `.env` отсутствует в git, есть `.env.example`

---

## 🧩 Архитектура и код

- [ ] Handlers не содержат бизнес-логики  
- [ ] Есть разделение на слои: `handler → usecase → repository`  
- [ ] Сервисы зависят от интерфейсов, а не от реализаций  
- [ ] В коде нет циклических зависимостей  
- [ ] Нет "божественных" файлов (`handlers.go` на 500 строк и т.п.)  
- [ ] Все импорты отформатированы (`goimports`)  
- [ ] Код проходит линтер (`golangci-lint run --fix`)

---

## 🧪 Тесты

- [ ] Есть юнит-тесты для бизнес-логики (usecase-level)  
- [ ] Тесты не зависят от порядка map или случайных данных  
- [ ] Тесты используют изолированную базу (in-memory / отдельный контейнер)  
- [ ] Есть базовое покрытие CRUD и edge-case (0/1 ревьювер, MERGED запрет и т.д.)  
- [ ] `make test` выполняется без ошибок

---

## 🧰 Инфраструктура

- [ ] Используется **PostgreSQL** (или другая заявленная БД)  
- [ ] Указана правильная версия Go (совпадает в `go.mod` и Dockerfile)  
- [ ] Multi-stage build в Dockerfile  
- [ ] Healthcheck или ping-эндпоинт доступен  
- [ ] Переменные окружения подгружаются безопасно (`godotenv` / `envconfig`)

---

## 🧠 Бизнес-логика

- [ ] При создании PR назначаются до 2 активных ревьюеров (без автора)  
- [ ] При `MERGE` изменение списка ревьюеров запрещено  
- [ ] Переназначение выбирает нового активного ревьювера из команды заменяемого  
- [ ] Идемпотентность `merge` соблюдена  
- [ ] Неактивные пользователи (`isActive=false`) не назначаются  
- [ ] Если нет кандидатов, PR создаётся без ревьюверов  
- [ ] Все edge-кейсы покрыты и проверены вручную

---

## 🔐 Безопасность и стабильность

- [ ] Нет настоящих секретов в репозитории  
- [ ] `.env` не содержит токенов/паролей  
- [ ] Merge-операция атомарна (через транзакцию)  
- [ ] Ошибки сравниваются через `errors.Is()`  
- [ ] Есть graceful shutdown и контекст (`context.Context`)  
- [ ] API возвращает осмысленные ошибки (JSON с кодом и сообщением)

---

## 📈 Дополнительно (повышает оценку)

- [ ] Есть эндпоинт статистики (`/stats`)  
- [ ] Проведено нагрузочное тестирование (описано в README)  
- [ ] Добавлена массовая деактивация пользователей с переназначением PR  
- [ ] Есть интеграционные или E2E тесты  
- [ ] Настроен линтер и pre-commit hook  
- [ ] Используется CI (GitHub Actions / GitLab CI)

---

✅ **Итоговая проверка перед пушем:**

- [ ] `make lint` — ✅  
- [ ] `make test` — ✅  
- [ ] `docker compose up` — ✅  
- [ ] Результаты и допущения описаны в `README.md`  
- [ ] Репозиторий публичен и запускается с нуля без ручных действий  